const nomes = ['igor', 'Fernanda', 'THIAGO', 'valdir', 'rodrigo']

const nomesAtualizados = nomes.map(nome => nome.toUpperCase())

console.log(nomesAtualizados)


//multiplicar todos os elementos de um array por 10
const arryNumeros = [1, 2, 3, 4]

const numerosAtualizados = arryNumeros.map(num => num * 10)

console.log(numerosAtualizados)

